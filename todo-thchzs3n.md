# Project TODO

- [x] Inspect the repository’s GitHub Pages source, build output, and workflow settings.
- [x] Correct the Pages publishing configuration so the site root includes a valid entry file.
- [x] Add or update automated coverage for the deployment configuration where practical.
- [x] Run build and test checks to verify the published artifact is valid.
- [x] Synchronize the verified Pages configuration to the linked GitHub repository and document the retained legacy publishing mode.
- [x] Publish the verified static build at the repository root as a fallback for legacy GitHub Pages.
- [x] Correct the GitHub Actions package-manager setup so future Pages builds can run successfully.
